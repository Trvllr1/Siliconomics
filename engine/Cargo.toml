[package]
name = "siliconomics-engine"
version = "0.1.0"
edition = "2021"
description = "Deterministic computation engine for Siliconomics semiconductor economics"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
